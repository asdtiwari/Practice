/*WAP to compare two integers*/
#include<stdio.h>
int main(void)	//cmp.c -> cmp.i -> cmp.exe
{
	int a,b;
	printf("\n Comparision \n\nEnter two numbers: ");
	scanf("%d%d",&a,&b);
	if(a>b)		//if(1)	1=TRUE
		printf("\n%d is greatest",a);
	else if()
		printf("%d is greatest",b);
	return 0;
}